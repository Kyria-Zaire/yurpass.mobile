# IP du PC sur le réseau local — pour qu'Expo Go (iPhone) se connecte au bundler.
# Copie ce fichier en .env et ajuste l'IP si besoin (ipconfig / ifconfig).
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.55
